# Project Status — P0002

- Status: active.
- Current stage: `STAGE_03` — Reference Strategy.
- Parent project: `P0001` (same truffle product; different creative direction).
- Duration target: 10 seconds.
- Aspect ratio target: 16:9.
- Clip count target: 1.
- Source-template mechanism: fixed 90-degree overhead invisible-chef food-commercial grammar.
- Primary product authority: current uploaded truffle-box photograph.
- Active video prompt package: **none**.

## Completed and retained
- `STAGE_00` Project Intake: immutable input metadata/provenance recorded.
- `STAGE_01` Source Prompt Analysis: source template transcribed and KEEP / ADAPT / REMOVE analysis exists.
- `STAGE_02` Product Identity: product identity and identity lock exist.

These upstream records remain useful and do not need to be discarded merely because the previous session advanced too far.

## Current gate — STAGE_03 Reference Strategy
The project must now decide the smallest reliable reference set before any creative scene synthesis or final video prompting.

Required decisions:
- whether the current original photo is sufficient as direct product authority;
- whether to reuse any already-approved P0001 references for the same physical product;
- whether a new clean top-down / white-background / macro / packaging reference is actually required for this creative direction;
- which reference controls geometry, texture, assortment, packaging, background, and final presentation;
- how to avoid leakage from the source photo's wooden tabletop and watermark.

No reference candidate is yet promoted into P0002's `approved_reference_set`.

## Premature downstream artifacts
The previous session created later-stage files before the upstream gates were complete:
- `07_SCENARIOS/selected/scenario_v01_invisible_chef_truffle_assembly.md`;
- `08_SHOT_DESIGN/timeline_v01.md`;
- `11_PROMPT_PACKAGES/PKG_GEMINI_VIDEO_V01/`.

These are preserved as historical **premature drafts** only. They are not active, not approved, and must not be used for generation until the project reaches those stages sequentially.

## Why the rollback happened
The prior workflow incorrectly treated a complete source/template prompt as permission to jump directly to a final Gemini video prompt. That violates the sequential production workflow. The project has been rolled back to the earliest incomplete gate without deleting historical work.

## Blocker
Reference strategy and reference readiness have not yet passed their gates.

## Next action
Complete the P0002 reference-role map first. Then, if a new reference image is truly necessary, generate only that reference image, register the Run, and perform Reference QA before moving to Creative Direction (`STAGE_06`). Do **not** generate the final video yet.
