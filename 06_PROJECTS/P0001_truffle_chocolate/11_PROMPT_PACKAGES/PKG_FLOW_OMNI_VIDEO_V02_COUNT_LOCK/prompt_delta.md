# Flow / Omni V02 — Character-count repair delta

Base prompt: `../PKG_FLOW_OMNI_VIDEO_V01/prompt.txt`

For the controlled V02 experiment, use the V01 prompt unchanged except insert the following block immediately after the `CHARACTER LOCK` section and before anatomy/zero-prop constraints.

```text
OFF-SCREEN POPULATION LOCK — CRITICAL

The EXACT THREE chefs visible at the beginning of the video are the ONLY human or human-like characters that exist anywhere in the entire physical world of this commercial.

TOTAL CHARACTER POPULATION OF THE ENTIRE WORLD = 3.

Not three visible plus hidden characters.
Not three in the foreground plus additional workers outside the crop.
Not three recurring chefs plus background chefs.

There are ONLY THREE characters in existence for all 10 seconds.

At frame 0, all three already exist and are already visible.

ZERO additional chefs exist:
- behind the camera;
- behind the hero truffle;
- behind neighboring truffles;
- outside the left edge;
- outside the right edge;
- deeper inside the box;
- near the rear box wall;
- outside the current crop;
- anywhere in the studio.

As the camera pulls backward and upward and reveals previously off-screen space, that newly revealed space may contain ONLY:
- existing truffles;
- existing dark paper cups;
- kraft cardboard box structure;
- empty dark studio background.

NEWLY REVEALED SPACE MUST NEVER CONTAIN A NEW CHEF OR HUMAN-LIKE FIGURE.

Do not reveal a fourth chef from the edge of frame.
Do not create a partial chef entering from outside the crop.
Do not duplicate one of the existing three chefs into newly visible space.
Do not treat Ingredient 5 as permission to spawn additional copies of the cast.

Ingredient 5 is IDENTITY EVIDENCE ONLY for the SAME THREE chefs already present in Ingredients 1, 2 and 3.
It must NEVER instantiate an additional character.

Character count is globally conserved:
3 at 00:00 = 3 at 00:02 = 3 at 00:05 = 3 at 00:09 = 3 at 00:10.

If widening the scene creates uncertainty about what exists outside the initial crop, reveal EMPTY KRAFT-BOX / PRODUCT SPACE rather than inventing another chef.
```

Everything else — ingredient stack, upload order, model, duration, aspect ratio and all exposed settings — must remain identical to V01 for the controlled comparison.
