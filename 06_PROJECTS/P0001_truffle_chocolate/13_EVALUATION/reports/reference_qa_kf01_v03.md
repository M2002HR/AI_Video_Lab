# P0001 KF01 v03 Camera-Derivation QA

## Scope
Two opening-keyframe candidates were generated by deriving a closer camera state from the already-selected combined scene master `P0001-R0015`, with `P0001-R0003` used only as product-identity backup.

Runs:
- `P0001-R0016` — selected
- `P0001-R0017` — alternate

This test intentionally avoided rebuilding the combined product+character world from separate product/character references.

## Result summary
### R0016 — SELECTED KF01 — ~4.5/5
Strengths:
- clearly closer than R0015;
- hero truffle dominates frame;
- exact three recurring chefs remain readable and differentiated;
- moustache / clean-shaven / goatee identity cues remain visible;
- zero tools, bowls, loose ingredients or debris;
- scene remains physically inside same kraft box;
- same hero truffle / cup / lighting language is preserved;
- camera relationship plausibly supports a later pull-back toward R0015.

Caveats:
- more neighboring assortment is visible than the strict target requested;
- static pose language persists somewhat (crossed arms / hands-on-hips / inspection posture);
- hero geometry remains mildly regularized relative to the handmade source.

Decision: PASS / SELECT as `KF01`.

### R0017 — APPROVED ALTERNATE — ~4.4/5
Strengths:
- strong close framing and hero dominance;
- scene/world continuity with R0015 preserved;
- exact three chefs; no props or loose ingredients;
- all faces readable.

Caveats:
- Character B and Character C use very similar hand-on-chin poses, reducing cast differentiation;
- same mild product regularization;
- neighboring assortment remains more visible than ideal.

Decision: PASS WITH CAVEATS / retain alternate.

## Comparative conclusion
R0016 is preferred because the three-character silhouette/pose language remains more differentiated while preserving the same successful camera-derived scene continuity. The difference is modest; both runs demonstrate that deriving an adjacent camera state from a selected scene master is materially more reliable than independently synthesizing the scene from many references.

## Storyboard mapping after v03
- `KF01` (~00:01): `P0001-R0016`
- `KF02` / scene master (~00:05): `P0001-R0015`
- `KF03` (~00:09.2): next task, derive a wider/higher hero frame from R0015 rather than synthesize from scratch.

## Gate status
KF01 gate: **PASSED**.

Proceed to KF03 camera derivation. Do not regenerate KF02; R0015 is the accepted mid-state anchor.
