# Glossary

Run = traceable AI execution. Prompt package = self-contained package for an external tool. Identity lock = product constraints that must remain stable. Reference role = explicit allowed influence of an image/asset. Gate = condition for stage progression. Canonical = validated prompt/version. Registry = generated CSV/index view.
