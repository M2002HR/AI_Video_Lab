# Multi-Clip Sequences

Use this folder only for multi-clip projects. Create a sequence folder such as `P0002-S01/` containing a Master Sequence, Clip Contracts, boundary records, and assembly/sequence-QA records.

Runs remain in project `12_RUNS/` and link via `sequence_id` + `clip_id` so the registry remains unified.
