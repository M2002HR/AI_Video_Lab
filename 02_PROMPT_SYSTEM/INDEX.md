# Prompt System

Canonical prompts are reusable production assets. `validated` may be default; `candidate` is not canonical until evidence is sufficient. Keep the registry synchronized with object-level prompt files. `PRM-SCN-ARCH-001_v1.0.0.md` is the active candidate for Process State Map + duration viability + adaptive Scenario Architecture Menu and remains a candidate until broader validation.
